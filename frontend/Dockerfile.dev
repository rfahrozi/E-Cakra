# Dockerfile.dev — Frontend Development (Vite dev server, hot reload)
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Tidak COPY source — pakai volume mount untuk hot reload

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
